ARG BUILDER_IMAGE=gitlab-registry.cern.ch/scipp/mario-mapyde/delphes:master
FROM ${BUILDER_IMAGE} as builder

USER root
WORKDIR /home

SHELL [ "/bin/bash", "-c" ]

RUN python -m pip install --no-cache-dir pandas matplotlib

ENTRYPOINT ["/bin/bash", "-l", "-c"]
CMD ["/bin/bash"]
